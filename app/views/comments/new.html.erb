 <%= form_for(:comment, :url => {:action => 'create', :shop_id => @shop.id}) do |f| %>
    <%= render(:partial => 'form', :locals => {:f => f} ) %>
      <center><%= submit_tag('Post comment') %></center>

  <% end %>
