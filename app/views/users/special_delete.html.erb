<div class="contentblock">
	<div id="content_button_bar">
		<div class="content_button_single"><%= link_to("Back to list", {:action => 'index'}, :class => 'back-link') %></div>
	</div>
</div>     

<div class="contentblock">
 	 <%= form_for(:user, :url => {:action => 'special_destroy', :id => @suser.id}) do |f| -%>

    <p>Are you sure you want to permanently delete this user?</p>
	<center><h2><%= @suser.name %></h2>

    <div class="form-buttons">
      <%= submit_tag("Delete User") %>
    </div>
  	<% end %>
  	</center>
</div>